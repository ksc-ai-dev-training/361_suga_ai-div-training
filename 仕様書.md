# 格ゲーWeb 仕様書

> 現時点のソースコードから読み取れる「実装済みの仕様」を整理したドキュメント。
> 今後の変更はこのファイルを更新しながら進める。

## 1. 概要

ブラウザ（Canvas 2D）で動作する対戦格闘ゲーム。HTML + JavaScript（ESモジュール）のみで構成され、ビルドツールやフレームワークは使用していない。

- リポジトリ名: `361_suga_ai-div-training`
- 画面: `index.html` の `<canvas id="gameCanvas" width="1200" height="600">` に描画
- エントリーポイント: `js/main.js`

## 2. ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | canvas 要素・オンライン対戦の接続パネル（`#onlinePanel`）・PeerJSのCDN読み込み・スクリプト読み込み |
| `style.css` | 画面中央にcanvasを配置し、ウィンドウ幅に合わせて拡縮表示。`#onlinePanel`のスタイルも含む |
| `js/main.js` | ゲームループ起動、入力フレーム更新、canvasのリサイズ処理、オンライン対戦の接続パネルのDOM操作 |
| `js/game.js` | `Game` クラス。P1/P2生成、毎フレームの更新・当たり判定・描画を統括 |
| `js/player.js` | `Player` クラス。移動・ジャンプ・攻撃状態・描画 |
| `js/combat.js` | 技のフレームデータ、矩形当たり判定関数 |
| `js/stage.js` | 地面の描画・地面座標の定義 |
| `js/input.js` | キーボード入力の管理（押下中／押した瞬間の判定） |
| `js/inputSource.js` | ローカルキーボード／ネットワーク越しのリモート操作を共通インターフェースで扱う入力ソース抽象化 |
| `js/network.js` | オンライン対戦（簡易版）の接続管理（`NetworkSession`。PeerJSのラッパー） |
| `js/room.js` | ルーム機能（最大8人・対戦枠2つ・観戦・チャット）の接続管理（`RoomHost`/`RoomMember`。PeerJSと`NetworkSession`を使う） |
| `js/ui.js` | HUD（ポートレート・HPバー・キャラ名・タイマー・SUPERゲージ）・K.O./勝敗メッセージ・操作説明オーバーレイ・タイトル/オンラインロビー画面の描画 |
| `js/commandInput.js` | 波動拳などのコマンド入力（↓↘→）を判定するための方向入力バッファ |
| `js/projectile.js` | 波動拳の飛び道具（`Projectile`）の移動・当たり判定・描画 |
| `js/ai.js` | CPU（P2）の行動ルーチン（`CpuController`） |
| `js/assets.js` | キャラクター・背景の画像プリロード（`loadAssets()`） |

## 3. ゲームループ

`requestAnimationFrame` ベース。毎フレーム以下の順で処理（`js/main.js`）。

1. `game.update(dt)` — 入力処理・物理更新・当たり判定
2. `game.render()` — 描画
3. `updateInputFrame()` — 「押した瞬間」判定用に前フレームのキー状態を保存

## 4. 操作方法

対戦形式は基本的に **1P（プレイヤー） vs 2P（CPU/AI）**。通常はキーボード操作が可能なのはプレイヤー1（P1）のみで、プレイヤー2（P2）は`js/ai.js`の`CpuController`が操作する。タイトル画面で「VS 2P」を選んだ場合は同じキーボードでP2も後述の専用キー（`P2_KEYS`）で操作でき（詳細は7.4節「VS 2Pモード」を参照）、「ONLINE」を選んだ場合は別々のPC・ブラウザ間でネットワーク越しに対戦できる（詳細は7.4節「オンライン対戦（簡易版）モード」を参照）。

| キー（1P） | 動作 |
|---|---|
| `A` | 左に移動 |
| `D` | 右に移動 |
| `W` | ジャンプ（押した瞬間のA/D入力で前・後・垂直ジャンプを判定） |
| `U` | 弱パンチ (light) |
| `I` | 中パンチ (medium) |
| `O` | 強パンチ (heavy) |
| `J` | 弱キック (light) |
| `K` | 中キック (medium) |
| `L` | 強キック (heavy) |
| `S` | しゃがみ（押している間・接地中のみ）。波動拳コマンドの「↓」入力も兼ねる |
| `A`または`D`（facingと逆方向） | ガード（押している間・接地中・非攻撃中のみ）。後退移動も同時に行われる |
| `R` | 決着後のリスタート（決着前は無効） |
| `Esc` | 操作説明（ヘルプ）の表示切り替え |

移動方向の判定は「向いている方向 (`facing`) と同じなら前進、逆なら後退」。前進と後退で速度が異なる。

### ヘルプ表示（`Esc`キー）
- `Esc`キーで操作説明のオーバーレイ画面を開閉できる（`js/ui.js` の `drawHelpOverlay()`）。表示中はゲーム全体が一時停止する（`Game.update()`の冒頭で判定し、以降の処理をスキップ）
- 決着後の画面（K.O.表示）の上からでも開閉可能
- 表示内容は操作方法一覧のみで、`js/ui.js`の`HELP_LINES`配列にハードコードされている（コマンド技の詳細な入力猶予秒数などは載せていない簡易版）

### HUD（画面上部・下部の情報表示、`js/ui.js` の `drawHud()`）
往年の対戦格闘ゲームのHUD構成（ポートレート・キャラ名・タイマー・SUPERゲージ）を、独自デザインで再現している。

> **⚠️ 著作権への配慮**: 実在の格闘ゲーム（ストリートファイターシリーズ等）のキャラクター画像・グラフィックはそのまま使用していない。ポートレートは各プレイヤーの色を使った完全オリジナルの簡易アイコン（`js/ui.js` の `drawPortrait()`）。

- **ポートレート**: 画面左上/右上に60×60pxの色付き四角＋簡易的な顔アイコン、その上に「1P」「2P」ラベル
- **キャラ名**: ポートレート横のHPバーの下に「PLAYER」（P1）／「CPU」（P2）と表示（実在キャラクター名は使用しない）
- **タイマー**: 画面上部中央に残り秒数を表示。単なる飾りではなく、実際にカウントダウンする（後述）
- **SUPERゲージ**: 画面下部左右にゲージバーを表示。単なる飾りではなく、実際にダメージのやり取りで増減し、SA発動の可否に影響する（後述）

### ガード
- `facing`と逆方向のキー（例: 右向き中なら`A`）を押している間・接地中・非攻撃中・ヒット硬直中でなければ自動的にガード状態になる（`Game.updateGuardState()`）。移動もそのまま行われるため、下がりながら自然にガードする形になる
- しゃがみ中（`S`同時押し）でもガードは成立する（しゃがみガード。高中低の区別はない）
- ガード中に攻撃を受けると、通常のダメージの10%（`GUARD_CHIP_RATIO`）だけを受ける「削りダメージ」になり、ガード硬直（ガードストン）が発生する
- ガード中は見た目がわずかに暗い色になり、ガードヒット時は水色に点滅する（`js/player.js` の `draw()`）
- ガード硬直中もキーを押し続けていればガード状態は継続する（＝連続ガードが可能）

### 波動拳・スーパーアーツ(SA)コマンド
`S`（↓）→ `S+forward`（↘、facingが右なら`S+D`、左なら`S+A`）→ `forward`（→）の順に方向入力した状態で、0.5秒以内に `U`/`I`/`O` のいずれかを押すと、通常パンチの代わりに波動拳が出る（`js/commandInput.js` の `hasQuarterCircleForward`）。

この波動拳コマンド「↓↘→」に続けて`S`（↓）→ `forward`（→）をさらに入力した「↓↘→↓→」を1.0秒以内に入力してからパンチを押すと、より高威力なスーパーアーツ（SA・強化版波動拳）が出る（`hasSuperArtsMotion`）。**ただしSUPERゲージが満タンでないとSAは出せず**、コマンドが成立していても通常の波動拳になる（詳細は後述の「SUPERゲージ」参照）。

**波動拳・SAともに地上でのみ発生可能**（`tryPunchOrSpecial()`で`p1.isGrounded`を確認）。空中でコマンドが成立していても通常パンチになる。

> **⚠️ 暫定実装・要見直し**: 現状は「Sキー押下中」および「直前に↘入力があった0.25秒間（`MOTION_GRACE`）」は歩行そのものを止めることでコマンド入力中の意図しない前進を防いでいる（`js/game.js` の `isInSpecialMotionGrace()`）。しゃがみ状態は実装済みになったが、この歩行抑制ロジック自体はしゃがみ実装後も残っており、まだ整理されていない。

### 昇龍拳コマンド
`forward`（→、facingが右なら`D`、左なら`A`）→ `S`（↓）→ `S+forward`（↘）の順に方向入力した状態で、0.5秒以内（`DRAGON_PUNCH_WINDOW`、仮値）に `U`/`I`/`O` のいずれかを押すと、通常パンチの代わりに昇龍拳が出る（`js/commandInput.js` の `hasDragonPunchMotion`）。波動拳（↓↘→）とは逆順の「→↓↘」で、`Game.tryPunchOrSpecial()`内でSAより後・波動拳より先にチェックされる。

> **⚠️ SAより後にチェックする理由**: 当初は波動拳・SAより先に昇龍拳をチェックしていたが、SAコマンド「↓↘→↓→」をキーボードで入力する際、2つ目の「↓」から最後の「→」へ切り替える瞬間にSとDが一瞬同時押しになり、意図せず「↘」が記録されてしまうことがあった。その結果、入力バッファの末尾が昇龍拳コマンド「→↓↘」とも一致してしまい、SAを出そうとしても昇龍拳が誤発動する不具合が起きたため、判定順をSA→昇龍拳→波動拳に変更した。

昇龍拳は**地上でのみ発生可能**だが、波動拳・SAと異なり**しゃがみ中（`isCrouching`）でも成立する**。コマンド完成時点（`S+D`を押した瞬間）で必然的に`S`キーを押している＝しゃがみ入力中になるため、波動拳・SAと同じ`!p1.isCrouching`制限をかけると原理的に発生できなくなってしまうため。また波動拳・SAと違って自分の弾の有無（`hasActiveProjectile`）による制限も受けない（飛び道具ではないため）。

昇龍拳は飛び道具ではなく、本体に上方向まで届く打撃判定（`range`/`height`/`offsetY`で頭上をカバー）を持たせた対空技として実装している（`SPECIAL_DATA.shoryuken`、`js/combat.js`）。発生が早く高威力な反面、recoveryが長く外すと反撃されやすいという、この系統の技によくある性格を仮値で再現している。参考にしたフレームデータ表には昇龍拳の記載が無いため、数値はすべて独自設定（仮値）。**無敵時間（発生時に相手の攻撃をすり抜ける性能）は未実装**——ヒット判定だけを持つ通常の打撃技として動作する。

2026年8月時点でCPU（P2）はこの技を使わない。CPUの対空反応（`js/ai.js`の`reactToJumpIn()`）は専用の対空技が無い前提で通常攻撃を代用しているが、この昇龍拳をCPUの対空選択肢に組み込む改修は行っていない（今後の拡張候補）。

### しゃがみ状態
- `S`キーを押している間・かつ接地中のみしゃがむ（空中ではしゃがめない。空中で`S`を押しても立ち高さのまま）
- しゃがみ中は`PUNCH_DATA`/`KICK_DATA`の代わりに`CROUCH_PUNCH_DATA`/`CROUCH_KICK_DATA`が使われる（`Game.handlePunchInput()` / `handleKickInput()`）
- しゃがみ中は波動拳・SAのコマンドは成立しない（`tryPunchOrSpecial()`で`!p1.isCrouching`を確認。しゃがみパンチが優先される）
- しゃがみ中は身長が220px→150pxに縮む（`Player.updateCrouch()`）。足元(地面についている位置)は変わらず、頭の高さだけ縮む。この150pxという値は表に記載がない独自の仮値

## 5. プレイヤー仕様（`js/player.js`）

### 見た目・画像アセット（`js/assets.js`, `Player.draw()`）
- `js/assets.js` の `loadAssets()` が起動時に以下の画像を読み込む。ファイルが存在しない/読み込みに失敗した場合は `null`（歩行コマは空配列）になり、その要素だけ次点の描画にフォールバックする（ゲームは止まらない）
  - `assets/background.png` — ステージ背景。canvasとアスペクト比が異なっても歪まないよう、中央基準で拡大・トリミングして表示する（`stage.js` の `drawImageCover()`。CSSの`background-size: cover`相当）
  - `assets/player1.png` / `assets/player2.png` — 立ち絵（右向き基準、透過PNG推奨）
  - `assets/player1_crouch.png` / `assets/player2_crouch.png` — しゃがみ絵（右向き基準、透過PNG推奨）。無い場合はしゃがみ中も立ち絵が表示される。ただし後述の遷移コマがある場合はこのファイルより遷移コマが優先され、実質未使用になる（P1は2026年8月時点でこのファイル自体を削除済み）
  - `assets/player1_walkN.png` / `assets/player2_walkN.png`（N=1,2,3...の連番） — 歩行コマ。存在する分だけ順番に読み込まれる（`loadNumberedFrames()`）。無い場合は歩行中も立ち絵が表示される
  - `assets/player1_crouch_transitionN.png` / `assets/player2_crouch_transitionN.png`（N=1,2,3...の連番） — 立ち⇔しゃがみの遷移コマ。無い場合は遷移演出なしで即座にしゃがみ絵に切り替わる
  - `assets/player1_guard.png` / `assets/player2_guard.png` — 立ちガード絵（右向き基準、透過PNG推奨）。無い場合はガード中も通常のポーズに暗い色のオーバーレイが乗るだけになる（P1は2026年8月時点でこのファイルを用意済み）
  - `assets/player1_hit.png` / `assets/player2_hit.png` — 被弾（ヒット反応）絵（右向き基準、透過PNG推奨）。無い場合はヒット硬直中も通常のポーズに赤いオーバーレイが乗るだけになる（P1は2026年8月時点でこのファイルを用意済み）
  - `assets/player1_down_launched.png` / `assets/player2_down_launched.png` — ダウン絵（吹き飛び中）。`assets/player1_down_lying.png` / `assets/player2_down_lying.png` — ダウン絵（倒れている間）。無ければ被弾絵、それも無ければ通常のポーズで代用する（P1は2026年8月時点で両方用意済み）
  - `assets/player1_ko.png` / `assets/player2_ko.png` — K.O.絵（HPが0になった瞬間から次のラウンド開始まで表示）。無ければダウンの倒れ絵→吹き飛び絵→被弾絵の順で代用する（P1は2026年8月時点で用意済み）
  - `assets/player1_jump_{phase}N.png` / `assets/player2_jump_{phase}N.png`（phase=`anticipation`/`rise`/`apex`/`fall`、N=1,2,3...の連番） — ジャンプポーズ。無い場合は従来通り立ち絵/しゃがみ絵のままジャンプする（P1は2026年8月時点で`anticipation`2枚・`rise`/`apex`/`fall`各1枚を用意済み）
  - `assets/player{1,2}_{type}_{strength}_{phase}{N}.png` — 攻撃ポーズ絵。`type`は`punch`/`kick`/`special`、`strength`は`light`/`medium`/`heavy`（`special`の場合は`hadouken`/`hadoukenSuper`/`shoryuken`）、`phase`は`startup`/`active`/`recovery`、`N`は1,2,3...の連番（例: `player1_special_hadouken_startup2.png`）。存在する組み合わせだけ読み込まれる（`loadAttackSprites()`）。無い技は今まで通り立ち絵のまま攻撃する。`recovery`は専用画像が無ければ`active`の最終コマを流用する
- 画像は「足元基準・アスペクト比維持」で表示される（`Player.drawSprite()`）。ハートボックス（当たり判定）のサイズとは独立しており、見た目のサイズが判定と厳密には一致しない
- 表示の高さは常に立ち高さ（`STAND_HEIGHT`=220px）基準の固定値。しゃがみ中でもハートボックスの高さ（150px）に合わせて画像側まで縮小されることはない（足元の位置だけは実際のハートボックスに追従する）
- 横幅が高さの90%（`MAX_SPRITE_ASPECT`）を超える画像は、横幅の方を縮めて表示する（仮値）。スカーフ等の装飾で横に広がった画像でも、もう一方のキャラクターと見た目のサイズを揃えるための処置。画像全体を均等に縮めるため、横に広い画像は多少縦長に圧縮されて見える
- `facing === -1`（左向き）のときは`ctx.scale(-1, 1)`で画像を水平反転する。画像自体は右向き1方向のみ用意すればよい
- ヒットフラッシュ・ガードフラッシュ・ヒット硬直・ガード中の色変化は、画像がある場合は半透明のオーバーレイとして重ねる（`Player.getStateTint()` + `source-atop`合成）。画像が無い場合（図形描画）は元通り不透明色で塗りつぶす
- 実在の格闘ゲームキャラクターの画像はそのまま使用できない（著作権）。オリジナルのデザインであることが前提

### 歩行アニメーション（`Player.getCurrentSprite()`, `WALK_FRAME_DISTANCE`）
- 地上で実際に移動している間（攻撃中・硬直中・空中でない）だけ`isWalking = true`になり、移動した距離を`walkCycleDistance`に積算する
- 歩行コマ画像が1枚以上あれば、`walkCycleDistance`を30px（`WALK_FRAME_DISTANCE`、仮値）ごとに区切ってコマを順番に切り替える（時間ベースではなく移動距離ベースなので、前進と後退で速度が違っても足の動きが滑る/滑らないという違和感が出にくい）
- 歩行コマは前進の歩き（1→2→3→...の順）を撮影したもの。後退中（移動方向が`facing`と逆）は同じコマを逆順に再生する（`frameCount - 1 - index`）ことで、新しい画像を用意せずに自然な後退の足運びに見せている
- 攻撃中・停止中・空中では`walkCycleDistance`が0にリセットされ、次に歩き始めたときは必ず1コマ目（前進側の先頭）から始まる
- 優先順位は「ダウン絵（ダウン中） > 攻撃ポーズ絵 > 被弾絵（実際にヒットを食らった瞬間のみ・しゃがみ中を除く） > 立ちガード絵（実際に攻撃を防いだ瞬間のみ・しゃがみ中を除く） > ジャンプポーズ絵（ため中・空中） > 立ち⇔しゃがみ遷移コマ/しゃがみ絵 > 歩行コマ > 立ち絵」（`getCurrentSprite()`）

### ガードポーズ（`Player.guardSprite`, `Player.isBlockingHit`, `getCurrentSprite()`）
- ガード絵は「ガード構え中（`isGuarding`＝後方入力中）」全般ではなく、**実際に攻撃を防いだ瞬間（ガード硬直中＝`blockstunTimer > 0`）だけ**表示する（`isBlockingHit`ゲッター）。後退で下がっているだけのとき（`isGuarding`のみtrueで`blockstunTimer`は0）はガード絵にならず、従来通りの立ち絵/歩行コマのまま
- `isCrouching`でない（しゃがみガードでない）場合のみ対象。しゃがみガード中は専用絵を用意していないため、従来通りしゃがみ絵（遷移コマ/`crouchSprite`）が優先される
- ガード絵を表示しているときは、通常ガード構え中にかかる暗いオーバーレイ（`getStateTint()`の`isGuarding`分岐）は重ねない。ガード絵自体で状態が分かるため

### 被弾ポーズ（`Player.hitSprite`, `Player.isTakingHit`, `getCurrentSprite()`）
- 実際に攻撃をガードせず食らってヒット硬直中（`hitstunTimer > 0`）のときだけ`hitSprite`があれば表示する（`isTakingHit`ゲッター）。ガード成立時（ガード硬直）はこちらではなく上記のガード絵側が使われる（`applyHit()`でヒット硬直とガード硬直は排他的に設定されるため同時には起こらない）
- `isCrouching`でない場合のみ対象。しゃがみ中に被弾したときの専用絵は無いため、従来通りしゃがみ絵が優先される
- 被弾絵を表示しているときは、通常ヒット硬直中にかかる赤いオーバーレイ（`getStateTint()`の`hitstunTimer`分岐）は重ねない。被弾絵自体で状態が分かるため。ヒットの瞬間の白フラッシュ（`hitFlashTimer`、0.15秒）は被弾絵の有無に関わらずこれまで通り最優先で重ねて表示される

### ジャンプポーズ（`Player.jumpSprites`, `Player.getJumpSprite()`）
- ジャンプは「ため（`anticipation`、地上・`isJumpSquatting`中）→ 上昇（`rise`、空中・上向き速度が大きい間）→ 頂点付近（`apex`、空中・上下速度が`JUMP_APEX_VY_THRESHOLD`=200px/秒未満）→ 下降（`fall`、空中・下向き速度が大きい間）」の4フェーズに分け、それぞれ専用のポーズ絵があれば表示する
- `anticipation`はため時間（`jumpSquatTimer`）の経過割合に応じて用意されたコマを順番に切り替える（攻撃ポーズの`startup`と同じ考え方）。`rise`/`apex`/`fall`はそれぞれ最後のコマを表示し続ける（1枚しか無ければ常にその1枚）
- `rise`/`apex`/`fall`の切り替えは`vy`（上下速度）の符号と大きさだけで判定する単純なもので、実際の滞空時間や高さは見ていない（仮の閾値）
- どのフェーズも専用絵が無ければ、次点（従来通りの立ち絵/しゃがみ絵）にフォールバックする。着地後の専用の「着地硬直」絵は用意していないため、着地した瞬間から即座に通常の立ち絵/歩行コマに戻る（着地時の行動制限も無い。以前からの挙動のまま）

### 立ち⇔しゃがみ遷移アニメーション（`Player.updateCrouchTransition()`, `CROUCH_TRANSITION_DURATION`）
- `crouchTransitionProgress`（0=立ち 〜 1=しゃがみ切り）を、`S`キーの押下状態（＝`isCrouching`）に応じて0.15秒（`CROUCH_TRANSITION_DURATION`、仮値）かけて滑らかに増減させる
- 遷移コマ画像があれば、進捗に応じて何コマ目を表示するか決める（例: 2コマなら進捗0〜50%/50〜100%で1・2コマ目）。しゃがむときは1→2の順、立ち上がるときは進捗が減っていくので自動的に2→1の順（逆再生）で表示される
- **遷移コマがある場合、専用の`crouchSprite`（しゃがみ絵）は使わない**。進捗が1に達したときは遷移コマの最後の1枚をそのまま「しゃがみ姿勢」として表示し続ける（P1は2026年8月時点でこの構成）
- 遷移コマ画像が無いキャラクター（2026年8月時点でP2）は、この遷移演出を使わず`isCrouching`に応じて`crouchSprite`へ即座に切り替わる（従来通りの挙動）
- ハートボックスの高さ自体（`updateCrouch()`）はこの遷移とは独立していて、`S`キーを押した瞬間に即座に切り替わる。見た目の遷移中でも当たり判定はしゃがみ後の高さになっている点に注意

### 攻撃ポーズ絵（`Player.getAttackSprite()`）
- 攻撃中、`{技のtype}_{strength}`（例: `punch_heavy`、`special_hadouken`）に対応するポーズ絵があれば、フェーズに応じて切り替えて表示する
  - `startup`フェーズ: そのフェーズの経過時間の割合（0〜1）に応じて、用意された`startup`コマを順番に切り替える（例: 3コマなら進捗0〜33%/33〜67%/67〜100%で1・2・3コマ目）。1枚しか無ければ常にその1枚
  - `active`フェーズ: `active`の最終コマを表示し続ける（無ければ`startup`の最終コマ）
  - `recovery`フェーズ: `recovery`専用のコマがあればその最終コマを表示する。無ければ（従来通り）`active`の最終コマを流用する
- 2026年8月時点で用意されているのはP1のみ:
  - 弱パンチ（`punch_light`）: `startup`1枚（構え）、`active`1枚（ジャブの伸びきり）
  - 強パンチ（`punch_heavy`）: `startup`1枚（振りかぶり）、`active`1枚（伸びきり）
  - 波動拳（`special_hadouken`）: `startup`3枚（構え→溜め→突き出しの3段階）、`active`1枚（放出の瞬間）
  - 昇龍拳（`special_shoryuken`）: `startup`1枚（沈み込んで拳を引く）、`active`1枚（拳を突き上げる）、`recovery`1枚（落下中の姿勢。P1ジャンプの`fall`ポーズと同一画像を流用）
  - それ以外の技・P2側は未対応で、後述の「攻撃モーション（見た目のみ）」による前後移動のみが適用される

### 攻撃モーション（見た目のみ、`Player.getAttackVisualOffset()`）
- 専用のパンチ/キックポーズ画像が無い場合の代替として、攻撃中はキャラクター全体（スプライトまたは図形、当たり判定の黄色い表示枠も含む）を技のリーチ(`range`)に応じた分だけ前後に動かす
- `startup`で踏み込み始め（最大量の50%まで）→`active`で最大まで突き出し→`recovery`で元の位置に戻る、という3段階のイージング
- `range`が0の技（波動拳・SAなど、その場で出す技）は動かない
- **この動きは完全に見た目だけ**で、実際の当たり判定（`getHitbox()`・`getHurtbox()`）の座標計算には一切影響しない
- 専用の攻撃ポーズ絵がある技でも、この前後移動は重ねてかかる（ポーズ絵＋移動の組み合わせ）

### サイズ・初期配置
- 幅100px × 高さ220px（しゃがみ中は150px）のハートボックス（当たり判定用の矩形。画像がある場合、見た目のサイズはこれと異なる場合がある）
- P1: x=250, facing=1（右向き） / P2: x=850, facing=-1（左向き）
- 地面座標 `GROUND_Y = 590`

### 移動・ジャンプの数値
| パラメータ | 値 |
|---|---|
| 前進速度 (`MOVE_SPEED`) | 250 px/秒 |
| 後退速度 (`BACKWARD_MOVE_SPEED`) | 150 px/秒 |
| 前ジャンプ水平速度 (`FORWARD_JUMP_SPEED`) | 350 px/秒 |
| 後ろジャンプ水平速度 (`BACKWARD_JUMP_SPEED`) | 210 px/秒 |
| 押し合い速度 (`PUSH_SPEED`) | 100 px/秒 |
| 重力・上昇中 (`GRAVITY`) | 2800 px/秒² |
| 重力・下降中 (`FALL_GRAVITY`) | 2800 px/秒²（`GRAVITY`と同値。上昇と対称に落下） |
| ジャンプ初速 (`JUMP_VELOCITY`) | 1300 px/秒 |
| ジャンプの「ため」時間 (`JUMP_SQUAT_DURATION`) | 4/60秒（4F、仮値） |

- ジャンプは接地中かつ非攻撃中のみ可能
- ジャンプ入力（`W`）を受けても即座には浮き上がらず、まず`JUMP_SQUAT_DURATION`（4F、仮値）の「ため」を挟む（`Player.jump()` → `jumpSquatTimer`をセットするだけで、実際の速度付与・`isGrounded`解除は`updateJumpSquat()`がため時間の経過後に行う）。ため中は接地したままだが、移動・新しい攻撃はできない（`update()`内の移動処理、`startAttack()`双方で`isJumpSquatting`をガード）
- 踏み切り方向（前/後/垂直）は、ジャンプ入力を受けた瞬間の入力方向と`facing`で判定し、ため中に`facing`が変わっても結果がぶれないよう`pendingJumpDirection`/`pendingJumpFacing`として保持しておき、ため終了時にそれを使って水平速度を決める
- 踏み切った瞬間の入力方向で「前ジャンプ／後ろジャンプ／垂直ジャンプ」の水平速度が空中で固定される（空中制御なし）
- 攻撃中は移動不可（`update`内で攻撃中は`x`が更新されない）

### 向き（facing）
- 接地している側だけ相手の中心座標を見て自動で向きを更新（空中では固定）

### 当たり判定
- ヒットボックス: 攻撃の `active` フェーズ中のみ発生。技データの `range` / `height` / `offsetY` を基に、向いている方向の前方に生成
- ハートボックス: プレイヤー矩形そのもの（100×220、しゃがみ中は100×150）
- 判定関数 `rectsOverlap` によるAABB判定

### 押し合い（キャラクター同士の衝突）
- 双方が接地しているときのみ発生（空中では押し合わない＝ジャンプ軌道を一定に保つため）
- 重なり量を計算し、`PUSH_SPEED` を上限に押し出す。押している側も相手が譲った分しか進めない

### ヒットフラッシュ・体力（HP）
- 攻撃がヒットすると防御側が0.15秒間白く点滅（`HIT_FLASH_DURATION`）。ガード成功時は水色に点滅する（`GUARD_FLASH_DURATION`）
- 各プレイヤーは `maxHp = 10000` のHPを持ち、ヒットすると技の `damage` 分（ガード時は10%）だけ減少（`Game.applyHit()` → `takeDamage()`）

### ヒットストップ（`Game.hitStopTimer`, `js/combat.js`のHITSTOP_*）
- ヒットが成立した瞬間、`Game.triggerHitStop()`が呼ばれ、`hitStopTimer`がセットされる。通常ヒットは`HITSTOP_HIT_DURATION`（0.08秒、仮値）、ガード成功時は`HITSTOP_BLOCK_DURATION`（0.04秒、仮値）で、技の強さによらず一律固定
- `hitStopTimer > 0`の間、`Game.update()`は「タイマーの減算のみ行って即return」する（`js/game.js`の`update()`冒頭、`matchOver`判定の直後）。これにより両プレイヤーの移動・アニメーション・CPU思考・ラウンドタイマーなど**試合全体が完全に一時停止**する（ヒット瞬間の白フラッシュ等の演出もこの間止まって見えるため、インパクトの重みが強調される）
- 複数のヒットがほぼ同時に発生した場合、`triggerHitStop()`は新しい値と現在値の大きい方を採用する（加算して積み増しはしない）
- ダメージ・硬直・ノックバックなど「ヒットの結果」自体はヒットが成立したその場（同一フレーム）で即座に適用され、そのあとの見た目上の停止だけがヒットストップとして続く

### ノックバック（`Player.applyKnockback()`, `Player.knockbackVx`）
- ガードされなかった通常ヒット（`causesKnockdown`の有無を問わない）が成立すると、`Game.applyHit()`が防御側に`applyKnockback(direction)`を呼び、攻撃側から見て反対方向（＝防御側から見て攻撃側と逆方向）に初速`KNOCKBACK_INITIAL_SPEED`（500px/秒、仮値）を与える。ガードされた場合は弾き飛ばしなし（削りダメージ・ガード硬直のみ）
- 弾き飛ばし速度は`KNOCKBACK_FRICTION`（2500px/秒²、仮値）で毎フレーム減衰し、やがて0になって止まる（摩擦で滑って止まるイメージ）。技の威力によらず初速は一律固定
- `knockbackVx`による移動は`Player.update()`内で無条件に（`isStunned`や`isAttacking`の状態に関係なく）処理されるため、ヒットストン中・ダウン中でも自然に横滑りする。これは通常の横移動（`vx`。硬直中は`isStunned`でブロックされる）とは別枠の独立した速度として扱われている（ジャンプの`jumpVx`と同じ設計思想）
- 相打ち等で両者に同時にヒットが成立した場合、両者とも相手と反対方向へ弾かれるため、結果的に引き離される

### SUPERゲージ（`js/player.js`, `js/game.js`）
- 各プレイヤーは `maxSuperGauge = 100` のゲージを持つ（`superGauge`）
- 攻撃がヒット（ガード含む）すると、**攻撃側・防御側の両方**が `技のdamage × SUPER_GAUGE_GAIN_RATE`（仮値0.03）分だけゲージを獲得する（`Game.applyHit()`）
- ゲージが満タン（100）のときだけスーパーアーツ（SA）が発動でき、発動と同時に全消費される（`Player.hasFullSuperGauge()` / `consumeSuperGauge()`）。通常の波動拳はゲージを消費しない
- 満タンでSAコマンドを入力すると波動拳ではなくSAが優先して出る。満タンでない場合はコマンドが成立していても通常の波動拳になる
- CPUも同じ条件でゲージ管理される（`js/ai.js` の `performAction()` で `hasFullSuperGauge()` を確認してからSAを選ぶ）
- 画面下部の「SUPER」ゲージバーで残量を表示し、満タン時は色が変わる（`js/ui.js` の `drawSuperGauge()`）

### ラウンドタイマー（`js/game.js`）
- 1ラウンド99秒（`ROUND_TIME`）。`Game.update()`内で毎フレーム`timeRemaining`を減算し、画面上部中央に表示する（`js/ui.js` の `drawTimer()`）
- 0になった時点でどちらのHPも0でなければ「タイムアップ」で試合終了となり、その時点でHPが多い方が勝利（同値なら引き分け）。決着理由は`Game.endReason`（`"ko"` または `"timeup"`）に記録され、決着画面のヘッダーが「K.O.」/「TIME UP」で切り替わる（`drawMatchResult()`）
- `restart()`で`timeRemaining`は`ROUND_TIME`にリセットされる

### ヒットストン・ガードストン（`js/player.js`）
- ヒットを食らう（ガードしていない）と`hitstunTimer`が、ガードすると`blockstunTimer`がセットされる（`Player.applyHitstun()` / `applyBlockstun()`）。値は技ごとに`combat.js`で定義（後述の技データ表を参照）
- どちらかのタイマーが残っている間（`isStunned`）は、移動・ジャンプ・新しい攻撃の発生が一切できない（`update()` / `jump()` / `startAttack()`内でガード）
- ヒット硬直（`hitstunTimer`）が発生すると、自分が出しかけていた攻撃も中断される（`applyHitstun()`内で`this.attack = null`）。これによりカウンターヒットで動きが止まる挙動が再現される
- ガード硬直（`blockstunTimer`）中でも、後方キーを押し続けていればガード状態自体は継続する（＝連続ガードは可能。新たな行動だけができない）
- ヒット硬直中は防御側の色が`#994444`（赤みがかった色）になり、反撃のチャンスであることが視覚的にわかる（`js/player.js` の `draw()`）
- ヒットストン中の相手に別の攻撃を当てると、単純に`hitstunTimer`が上書きされて硬直が延びるため、結果的に簡易的なコンボ（連続ヒット）が成立する。ただしノックバックや専用のコンボ補正（ダメージ減衰など）は無い

### ダウン（転倒）状態（`Player.downTimer`, `applyKnockdown()`）
- 技データに`causesKnockdown: true`が付いている技（2026年8月時点で`CROUCH_KICK_DATA.heavy`のみ）をガードせずに食らうと、通常のヒットストンの代わりに`Player.applyKnockdown()`が呼ばれ、`downTimer`が`KNOCKDOWN_LAUNCHED_DURATION + KNOCKDOWN_LYING_DURATION`（0.35秒＋1.0秒、いずれも仮値）にセットされる。同時に`hitstunTimer`/`blockstunTimer`は0にリセットされ、出しかけの攻撃も中断される（`this.attack = null`）
- `downTimer > 0`の間は`isStunned`が`true`になる（`isStunned`の判定条件にヒットストン・ガードストンと並んで`downTimer > 0`が含まれる）ため、移動・ジャンプ・新しい攻撃の発生・ガード開始（`Game.updateGuardState()` / `CpuController.updateGuard()`でそれぞれ`downTimer <= 0`も条件に追加済み）が一切できない、という既存の硬直系システムをそのまま流用している
- ダウン中はさらに2つの局面に分かれる（`Player.isKnockdownLaunched`ゲッターで判定）:
  1. **吹き飛び局面**（`downTimer`が`KNOCKDOWN_LYING_DURATION`を超えている間、最初の0.35秒）: `downLaunchedSprite`（吹き飛んでいるポーズ絵）を表示
  2. **倒れ局面**（残り1.0秒）: `downLyingSprite`（倒れているポーズ絵）を表示
- ダウンから復帰する際の専用アニメーション（起き上がりモーション）は無く、`downTimer`が0になった瞬間に即座に通常の立ち絵/操作可能状態へ戻る（ヒットストン・ガードストンからの復帰と同様、瞬間的な切り替え）
- **ダウン中は無敵**（`isDowned`）。`Game.checkAttackHit()` / `checkProjectileHits()`の両方で、防御側が`isDowned`の間はそもそも当たり判定を調べず、`applyHit()`自体が呼ばれない（＝ダメージも`downTimer`の上書きも発生しない）。通常技も飛び道具も、ダウン中の相手をすり抜ける（弾は消費されずそのまま飛び続ける）
- ダウン専用のポーズ絵は`getCurrentSprite()`の最優先（攻撃ポーズ絵よりも上）で判定される。`downLaunchedSprite`/`downLyingSprite`が無ければ被弾絵（`hitSprite`）で代用し、それも無ければ通常のポーズ絵にフォールバックする。画像が一切無い場合の図形描画（`drawRect()`）ではダウン中はグレー（`#555555`）で塗りつぶす
- 倒れポーズ絵（`downLyingSprite`）だけは、他のポーズ絵より少し大きく表示される（`DOWN_LYING_SCALE`=1.2倍、仮値）。`Player.draw()`で表示中のスプライトが`downLyingSprite`と同一かどうかを見て、`drawSprite()`の表示高さ（通常は`STAND_HEIGHT`固定）に倍率をかけるかどうかを切り替えている
- 倒れポーズ絵は画像下部に大きな余白（実測で画像高さの約25%）があり、そのまま足元アンカーに合わせると浮いて見えるため、`DOWN_LYING_Y_OFFSET`（66px、仮値。表示高さ×余白比率から算出）の分だけ表示位置を下にずらして補正している（`drawSprite()`の`yOffset`引数。当たり判定・実際の座標には影響しない、見た目だけの補正）
- ダウン中もハートボックスのサイズ自体は変更しない（立ち姿勢と同じ矩形のまま）。しゃがみと違い、見た目が寝ていても当たり判定は立ち姿勢基準のまま、という「見た目とハートボックスの分離」方針を踏襲している
- 2026年8月時点で用意されているのはP1のみ（`assets/player1_down_launched.png` / `player1_down_lying.png`）。P2側は未対応で、ダウン中も専用絵が無いためフォールバック描画になる

### K.O.演出（`Player.isKO`, `assets/player1_ko.png`）
- `Player.isKO`は`this.hp <= 0`のシンプルなゲッター。特別なタイマーは持たず、HPが0になった瞬間から次のラウンド開始（`startRound()`で`createPlayers()`により作り直され`hp`が`maxHp`に戻る）まで`true`のままになる
- `isKO`は`getCurrentSprite()`の中で**最優先**（ダウン絵・攻撃ポーズ絵よりも上）に判定される。`koSprite`（K.O.専用のうつ伏せ倒れ絵）があればそれを表示し、無ければダウンの倒れ絵（`downLyingSprite`）→吹き飛び絵（`downLaunchedSprite`）→被弾絵（`hitSprite`）の順にフォールバックする。画像が一切無い場合の図形描画（`drawRect()`）ではダウン中と同じグレー（`#555555`）で塗りつぶす
- HPが0になった瞬間、`Game.checkRoundEnd()`によりそのフレームで即座に`introPhase = "roundEnd"`へ遷移する。以降`Player.update()`は呼ばれなくなる（`introPhase !== "playing"`の間は通常のゲーム更新自体が止まるため）ので、K.O.になった瞬間の位置・姿勢のまま画面に固定表示され続ける（`isKO`はhpを見るだけのゲッターなので、`update()`が止まっていても`render()`のたびに正しく`true`を返す）
- K.O.絵は横に長い「うつ伏せに倒れる」構図のため、他のポーズ絵に適用している横幅上限（`MAX_SPRITE_ASPECT`=0.9）を`koSprite`表示時だけ無効化し（`Player.draw()`で`maxAspect`を`Infinity`にして`drawSprite()`に渡す）、実際のアスペクト比のまま歪まずに表示する。また実寸が大きい（横長な）画像のため、他のポーズより縮小して表示する（`KO_SCALE`=0.6倍、仮値）
- 決着表示（「誰が勝ったか」）は本機能とは別に既に実装済み。ラウンド決着時は`drawRoundResult()`（`introPhase === "roundEnd"`の間、暗転なしの軽量表示）、マッチ全体の決着時は`drawMatchResult()`（`matchOver`の間、暗転＋「YOU WIN / CPU WINS / DRAW」＋獲得ラウンド数のスコア＋「Rキーでリスタート」）が表示される（詳細は前述の「試合開始演出」「勝敗判定・ラウンド制」の章を参照）。K.O.絵はこれらの決着表示の背後で倒れた姿として表示され続ける
- 2026年8月時点で用意されているのはP1のみ（`assets/player1_ko.png`）。P2側は未対応でフォールバック描画になる

## 6. 攻撃仕様（`js/combat.js`）

フレームデータは60fps基準の「フレーム数」で定義し、秒に変換して使用。`startup`（発生）→ `active`（持続）→ `recovery`（硬直）の3フェーズで管理。

以下の発生・持続・硬直・ダメージ・硬直差は、外部のフレームデータ表（ユーザー提供のスクリーンショット）の値と一致させている。`range` / `height` / `offsetY`（当たり判定のサイズ・位置）は表に記載がないため、既存の値をそのまま使用している。

ヒットストン／ガードストンは表に直接載っていないため、`硬直差 = 相手の硬直 - 自分のrecovery` の関係から逆算している（`相手の硬直 = 表の硬直差 + recovery`）。

### 立ちパンチ (`PUNCH_DATA`)
| 技 | 発生 | 持続 | 硬直 | リーチ | 高さ | Y位置 | ダメージ | ヒットストン | ガードストン |
|---|---|---|---|---|---|---|---|---|---|
| light（弱P・ジャブ） | 4F | 3F | 7F | 45px | 40px | 70px | 300 | 11F | 6F |
| medium（中P・鉤突き） | 6F | 4F | 11F | 60px | 50px | 60px | 600 | 18F | 10F |
| heavy（強P・正拳突き） | 10F | 5F | 18F | 75px | 60px | 50px | 800 | 22F | 16F |

### 立ちキック (`KICK_DATA`)
| 技 | 発生 | 持続 | 硬直 | リーチ | 高さ | Y位置 | ダメージ | ヒットストン | ガードストン |
|---|---|---|---|---|---|---|---|---|---|
| light（弱K・ローキック） | 5F | 3F | 11F | 55px | 45px | 130px | 300 | 13F | 7F |
| medium（中K・横蹴り） | 9F | 3F | 18F | 70px | 55px | 120px | 700 | 22F | 14F |
| heavy（強K・後ろ回し蹴り） | 12F | 4F | 20F | 85px | 65px | 110px | 900 | 29F | 21F |

### しゃがみパンチ・しゃがみキック (`CROUCH_PUNCH_DATA` / `CROUCH_KICK_DATA`)
`S`キー押下中（かつ接地中）に使用される。`range` / `height` / `offsetY` は表に記載がないため既存の値を踏襲している。

| 技 | 発生 | 持続 | 硬直 | ダメージ | ヒットストン | ガードストン |
|---|---|---|---|---|---|---|
| しゃがみ弱P（ジャブ） | 4F | 2F | 9F | 300 | 13F | 8F |
| しゃがみ中P（ストレート） | 6F | 4F | 13F | 600 | 18F | 13F |
| しゃがみ強P（突き上げアッパー） | 9F | 6F | 22F | 800 | 23F | 15F |
| しゃがみ弱K（キック） | 5F | 2F | 10F | 200 | 13F | 9F |
| しゃがみ中K（くるぶしキック） | 8F | 3F | 19F | 500 | 20F | 13F |
| しゃがみ強K（回転足払い） | 9F | 3F | 23F | 900 | ダウン | 11F |

- しゃがみ強Kは表の硬直差「D」通り、ヒット時に相手をダウンさせる（`causesKnockdown: true`、後述の「ダウン状態」参照）。以前は未実装だったため長めのヒットストン（45F、仮値）で代替していたが、ダウン状態の実装に伴い本来の効果に置き換えた。`hitstun`フィールドの値自体は現在使われていない（ガードされた場合の`blockstun`とは別枠のため）

### 必殺技・スーパーアーツ (`SPECIAL_DATA`) — 波動拳
`isProjectile: true` を持つ技は、プレイヤー本体に打撃判定を持たせない（`Player.getHitbox()`が`null`を返す）。代わりに`active`フェーズに入った瞬間、`js/game.js`の`trySpawnProjectile()`が`projectile`設定を基に`Projectile`を1つ生成する。

参考にしたフレームデータ表には必殺技の記載がないため、発生・持続・硬直・ヒットストン・ガードストンは既存値のまま、ダメージのみ `maxHp = 10000` のスケールに合わせて仮設定している（通常技heavyの800〜900と近い威力をhadoukenに、その3倍程度をSAに割り当て）。

> **⚠️ ダメージ調整は保留中**: コマンド技（必殺技・SA）を全て実装し終えてから、まとめて火力バランスを調整する方針。現在の数値（hadouken:1000 / hadoukenSuper:3000）は仮の値であり、他のコマンド技が揃うまでは変更しない。

| 技 | コマンド | 発生 | 持続 | 硬直 | 弾速 | 弾サイズ | ダメージ | ヒットストン | ガードストン | 色 |
|---|---|---|---|---|---|---|---|---|---|---|
| hadouken | ↓↘→+P | 13F | 0.05秒 | 0.35秒 | 600px/秒 | 40×30px | 1000 | 25F | 14F | 水色 |
| hadoukenSuper（SA） | ↓↘→↓→+P | 18F | 0.06秒 | 0.5秒 | 700px/秒 | 70×50px | 3000 | 40F | 22F | オレンジ |

- SA（スーパーアーツ）は波動拳コマンド「↓↘→」に続けて「↓→」を追加入力した形。`hasSuperArtsMotion()`（`js/commandInput.js`）が`Game.tryPunchOrSpecial()`内で最優先（昇龍拳・波動拳より先）に判定され、成立していればそちらが発動する（優先順位の理由は前述の「昇龍拳コマンド」の注記を参照）
- コマンド入力バッファの保持時間は最大1.2秒（`RETENTION`）。波動拳の判定猶予は0.5秒、SAの判定猶予は1.0秒（`SIMPLE_WINDOW` / `SUPER_WINDOW`）
- 飛び道具は画面外に出ると消滅（`Projectile.update()`）
- 発射者（`owner`）自身には当たらない
- 自分の弾（通常・SA問わず）は同時に1発まで（画面上に自分の弾が残っている間はコマンドを入力しても通常パンチになる。`hasActiveProjectile()`）
- 持ち主が異なる弾同士が重なると、`damage`（威力）が高い方が生き残り、低い方だけ消滅する（`Game.checkProjectileClashes()`。プレイヤーへの当たり判定より先に処理される）。同威力同士がぶつかった場合のみ、双方とも消える「相殺」になる。この仕組みにより、SA（3000）は通常の波動拳（1000）を消しながら進み続ける

### 必殺技 (`SPECIAL_DATA`) — 昇龍拳
`isProjectile`を持たない（=falsy）ため、`hadouken`系とは異なりプレイヤー本体が通常の打撃技と同じ経路で当たり判定を持つ（`Player.getHitbox()`が`range`/`height`/`offsetY`から矩形を計算する、通常のパンチ・キックと同じロジック）。飛び道具は生成されない。

| 技 | コマンド | 発生 | 持続 | 硬直 | リーチ | 高さ | ダメージ | ヒットストン | ガードストン |
|---|---|---|---|---|---|---|---|---|---|
| shoryuken | →↓↘+P | 5F | 10F | 32F | 150px | 230px | 2000 | 30F | 18F |

- 参考にしたフレームデータ表には昇龍拳の記載がないため、数値はすべて独自設定（仮値）。発生が早く高威力な反面、recoveryが長く外すと反撃されやすい、という昇龍拳系統の技によくある性格を意識した値にしている
- `offsetY`に負の値（-40px）を使い、頭よりさらに上から当たり判定を出すことで、ジャンプしてくる相手を迎撃しやすい（対空向けの）大きい縦長の判定にしている。リーチ（150px）・高さ（230px）ともに他の通常技よりかなり広め（強キックの85pxの倍近く）にして、対空として機能しやすくしている
- **無敵時間（発生時に相手の攻撃をすり抜ける性能）は未実装**。当たり判定だけを持つ通常の打撃技として動作するため、対空として振っても相打ちになりうる
- コマンド入力の判定猶予は0.5秒（`DRAGON_PUNCH_WINDOW`、仮値、`js/commandInput.js`）
- 2026年8月時点でCPU（P2）はこの技を使わない（`js/ai.js`の対空反応は通常攻撃を代用したままで、昇龍拳への差し替えは未実施）

#### 自ら浮き上がる演出（`SPECIAL_DATA.shoryuken.selfLaunchVelocity`, `Player.startAttack()`）
- `startAttack()`は、技データに`selfLaunchVelocity`があれば、攻撃開始と同時に`this.vy = -selfLaunchVelocity`・`this.isGrounded = false`をセットする（`this.jumpVx`は0にして水平方向には飛ばない、その場で上下するだけの動きにする）。以降は通常のジャンプと同じ重力（`GRAVITY`=上昇中2800px/秒²、`FALL_GRAVITY`=下降中2800px/秒²、現在は同値）で自然に放物線を描いて上昇→下降する
- 昇龍拳の`selfLaunchVelocity`は1600px/秒（仮値）。通常ジャンプの`JUMP_VELOCITY`(1300px/秒)より初速を大きくし、通常ジャンプより高く舞い上がるようにしている（滞空時間もその分長い）
- 攻撃中（startup/active/recovery）は`isAttacking`により通常の横移動処理がブロックされるため、上昇中に左右移動でそれることはない
- **recovery（32F≈0.53秒）が終わって`this.attack`が`null`に戻っても、上昇初速(1600px/秒)による滞空時間の方が長いため、着地までにはまだ余裕がある場合がある**。その間はジャンプ用のポーズ絵システム（`Player.getJumpSprite()`）が引き継ぎ、`vy`の符号に応じて空中ポーズを表示し続ける。着地判定・重力自体は`update()`内の共通処理でジャンプと変わらず自動的に行われるため、昇龍拳専用の着地処理は実装していない
- この兼ね合いにより、昇龍拳のrecoveryポーズ（`special_shoryuken_recovery1.png`）とジャンプの`fall`ポーズ用画像を同一にしている一方、`recovery`終了から着地までの間に一瞬だけジャンプの`apex`ポーズ（別画像）を経由することがある（`vy`がほぼ0付近でrecoveryフェーズが終わった場合）。ごく短時間のため大きな違和感にはなりにくいが、気になる場合は`selfLaunchVelocity`や`recovery`の値を調整して揃えることもできる（未調整・仮値のまま）

### 波動拳（通常技）の弾の見た目（`Projectile.sprites`, `js/assets.js`のhadoukenSprites）
- `assets/hadoukenN.png`（N=1,2,3...連番、P1/P2共通・プレイヤー非依存の1セット）を用意すると、通常の波動拳（`hadoukenSuper`ではない方）の弾がその画像で表示される。無ければ従来通り水色の楕円で描画する
- 表示サイズは当たり判定（40×30px）とは独立した固定の表示高さ（`SPRITE_DISPLAY_HEIGHT`=100px、仮値）で描画し、当たり判定の中心に画像の中心を合わせる（キャラクター立ち絵の「見た目とハートボックスの分離」と同じ考え方）
- 複数コマ用意すると`SPRITE_FRAME_DURATION`（0.08秒、仮値）ごとに順番に切り替わり、発光が揺らめくアニメーションになる（`Projectile.animTimer`を`update(dt)`で積算し、コマ番号は`Math.floor(animTimer / SPRITE_FRAME_DURATION) % コマ数`で決める。ループ再生）
- 画像は右向き（`direction===1`）基準で用意し、`direction===-1`（左向きに飛ぶ、P2が発射した場合など）のときは`ctx.scale(-1,1)`で水平反転する
- SA（`hadoukenSuper`）は今のところ専用の弾絵が無いため、従来通りオレンジ色の楕円のまま（`trySpawnProjectile()`で`a.strength === "hadouken"`のときのみ`hadoukenSprites`を渡している）

- 攻撃中は新しい攻撃を出せない（キャンセル不可）
- 1回のヒットにつき1度だけ判定（`hasHit`フラグ）
- ヒット判定自体は双方向（`checkAttackHit(p1, p2)` と `(p2, p1)` の両方を毎フレーム実行）。P2（CPU）も攻撃するようになったため、双方向とも実際に機能する

## 7. CPU（P2）の行動AI（`js/ai.js`）

`CpuController`は以下の4段階で構成される。

> **⚠️ ダウン状態は非対応**: ダウン（転倒）状態が未実装のため、相手の状態認識は「地上／空中／攻撃硬直中」までに留まる。対空も専用の必殺技（昇龍拳など）が無いため、通常攻撃で代替している。なお、SUPERゲージ（後述）はCPUも参照しており、満タンでない限りSAは選ばない。ただし②モード決定の「リソース」判断（攻める/逃げるの方針）にはHP差のみを使い、ゲージ量は考慮していない。

### ① 状況認識（`perceive()`, 毎フレーム）
- **距離**: プレイヤーとの水平距離。`rangeBucket()`で「近距離（110px以下）／中距離（500px以下）／遠距離」の3段階に分類する（`CLOSE_RANGE` / `FAR_RANGE`）
- **体力差**: `hpAdvantage = 自分のHP割合 - 相手のHP割合`（-1〜1、正なら有利）
- **位置取り**: 自分・相手それぞれが画面端から150px以内（`CORNER_MARGIN`）にいるかどうか（`isCornered()`）

### ② モード決定（`chooseMode()`, 2.5〜5秒ごと＝「モードの粘り」）
状況認識の結果から重み付き抽選で3モードのいずれかを選び、`MODE_MIN_DURATION`〜`MODE_MAX_DURATION`（2.5〜5秒のランダム値）の間は維持し続ける。頻繁に切り替えないことで機械的な動きを避けている。

- **aggressive（ガン攻め）**: HP有利（`hpAdvantage > 0.3`）や相手を追い詰めている（`opponentCornered`）と重みが増す
- **watch（待ち）**: 上記どちらにも該当しない場合の基本モード
- **retreat（逃げ・牽制）**: HP不利（`hpAdvantage < -0.3`）だと重みが増す。ただし自分が壁際（`selfCornered`）だと下がる余地がないため重みを下げる

### ③ 行動選択（`decide()`, `THINK_INTERVAL`=0.3秒間隔）
1. **反撃優先**（モード共通）: 相手が攻撃の硬直（`recovery`）中で近距離内なら、70%の確率（`PUNISH_CHANCE`）でモードを無視して攻撃を差し込む
2. それ以外は、**現在のモード×間合い（近/中/遠）**の組み合わせごとに用意された重み付け表（`ACTION_WEIGHTS`）から、`attack`（通常技） / `fireball`（波動拳・SA） / `jumpApproach`（ジャンプ接近） / `approach`（歩いて接近） / `retreat`（後退） / `wait`（様子見）のいずれかを確率で選ぶ
   - 例: aggressiveモードの中距離は「接近35 / 波動拳25 / ジャンプ接近25 / 様子見15」、watchモードの近距離は「攻撃30 / 様子見45 / 後退25」といった配分（値はいずれも仮のバランス）
   - 攻撃クールダウン中（`ATTACK_COOLDOWN`=0.5秒）・非接地中など、その場で選べない選択肢は抽選から自動的に除外される
   - `fireball`を選んだ場合、さらに15%の確率（`SUPER_CHANCE`）でSA（強化版）になる

攻撃中（`player.attack`がある間）やガード中は行動選択をスキップする。

### ④ 割り込み反応（`reactToThreat()`, 毎フレーム監視、モードの「意識」で反応率が変わる）
モードごとに「反応できる基本確率」への倍率（`MODE_ATTENTION`）を持つ。watchは警戒度が高く反応しやすい、aggressiveは攻めに意識が向いていて反応が甘い、retreatはその中間、という設計。

- **相手の飛び込み検知**（`reactToJumpIn()`）: 相手が地上→空中に変わった瞬間、間合い（`JUMP_IN_RANGE`=300px）以内なら、モードの`antiAir`確率（watch85% / retreat50% / aggressive25%）で通常攻撃を対空として繰り出す
- **相手の攻撃発生検知**（`reactToAttackStartup()`）: 相手の攻撃が「発生（`startup`）」した瞬間、間合い（`REACTION_RANGE`=220px）以内なら、モードの`guardEvade`倍率をかけた確率（`BASE_REACT_CHANCE`=80%が基準）で反応を試みる。反応できた場合、60%（`GUARD_VS_EVADE_RATIO`）でガード、残りはバックジャンプで離脱する。反応できなければそのまま食らう（読み負け）
- 同じ攻撃には二度反応しない。自分が行動中（`player.attack`がある）なら反応しない

- `restart()`時に`createPlayers()`経由で`CpuController`も作り直され、新しい`player1`/`player2`・`canvasWidth`を参照する
- 自分の飛び道具が画面上に残っているかは`Game`から注入される`hasOwnProjectile()`コールバックで判定する

### CPUが行わないこと（今後の課題）
- プレイヤーの入力パターンを学習する、フェイントを見切るといった高度な読み合い
- 難易度段階（弱いCPU/強いCPUの切り替え）
- コンボ・連携攻撃
- ダウン状態を使った判断（そもそもゲーム側に未実装のため）。SUPERゲージ量そのものを攻め/待ち/逃げの判断材料にすることはしていない（SA発動可否の確認にのみ使用）

## 7.4. タイトル画面（`Game.introPhase === "title"`, `js/ui.js`のdrawTitleScreen()）
- ゲーム起動直後は`introPhase = "title"`から始まる。この間はプレイヤー・CPUの更新は一切行われず、`js/game.js`の`update()`は「Enterキー入力チェック→即return」のみ実行する
- 画面には`assets/title_logo.png`（ロゴ画像。無ければ「KogaFighter」というテキストで代用）と、中央下寄りにSTART・PRACTICE・VS 2P・ONLINEの4つのボタンを横並びで表示する（`drawTitleScreen()`）。ボタンの矩形はそれぞれ`getTitleButtonRect()`/`getPracticeButtonRect()`/`getVersusButtonRect()`/`getOnlineButtonRect(canvasWidth, canvasHeight)`で計算し、描画側・クリック判定側の両方で共有する（内部的には`getTitleButtonRectAt(index, ...)`で4ボタンの横並び位置を算出する共通ヘルパーを使っている）
- どのボタンも**クリック**で押せる。STARTのみ**Enterキー**でも押せる（PRACTICE・VS 2P・ONLINEにキーボードショートカットは無い）。クリックは`js/main.js`でcanvasに登録した`click`イベントリスナーが、CSSで拡縮された表示座標を`canvas.width/height`基準の内部座標に変換（`toCanvasCoords()`）した上で`Game.handleClick(x, y)`に渡し、矩形内かどうかを`isPointInRect()`で判定する
- マウスがボタン上に乗っている間はカーソルが`pointer`に変わり、ボタンの色も明るくなる（`mousemove`イベント→`Game.setMousePosition()`→`render()`内で`isHoveringStartButton()`/`isHoveringPracticeButton()`/`isHoveringVersusButton()`/`isHoveringOnlineButton()`を見て描画色を切り替え）
- STARTが押されると`Game.startBattle("vs")`、PRACTICEが押されると`Game.startBattle("practice")`、VS 2Pが押されると`Game.startBattle("2p")`が呼ばれ、`Game.mode`をセットしたうえで`introPhase`が`"ready"`に進み、後述の「READY→FIGHT!」演出を経て対戦（または練習）が始まる。ONLINEが押されると`Game.showOnlineLobby()`が呼ばれ、`introPhase`が`"onlineLobby"`（後述）に進む
- `restart()`（決着後のRキー。`"vs"`/`"2p"`/`"online"`モードのみ発生。`"practice"`では決着自体が起きない）では`introPhase`はタイトルへは戻らず、直接`"ready"`から始まる（リマッチはタイトル画面を経由しない）

### VS 2Pモード（`Game.mode === "2p"`）
- タイトル画面のVS 2Pボタンから開始する、CPUの代わりにもう一人がキーボードで2P（`player2`）を操作するモード。同じキーボード・同じ画面でのローカル対戦専用（ネットワーク越しに別PCと対戦したい場合は後述の「オンライン対戦」モードを使う）
- `updateLocalSimulation()`内で`this.mode === "2p"`のときは、1P用の入力処理一式（`handleInput()` / `handleJumpInput()` / `updateCrouchState()` / `updateGuardState()` / `updateCommandBuffer()` / `handlePunchInput()` / `handleKickInput()`）を、`player2`・`this.player2LocalInput`（`P2_KEYS`を割り当てたローカル入力ソース）・専用のコマンドバッファ（`this.commandBuffer2`）を渡してそのまま2P用にも実行する。`this.cpu.update(dt)`は呼ばれない（CPUは行動しない）
- 2Pのキー割り当ては`P2_KEYS`（`js/game.js`）で定義:

  | 操作 | キー |
  |---|---|
  | 移動 | ← / → |
  | ジャンプ | ↑（←/→同時押しで前・後ジャンプ） |
  | しゃがみ | ↓ |
  | 弱 / 中 / 強パンチ | テンキー1 / 2 / 3 |
  | 弱 / 中 / 強キック | テンキー4 / 5 / 6 |

  波動拳・SA・昇龍拳などのコマンド技も、1Pと同じ方向入力パターン（↓↘→など）を2P側のキー（←/→/↑/↓）で入力すれば成立する（`updateCommandBuffer()`が`player.facing`を見て前後を判定するため、2Pがどちら向きでも正しく機能する）
- HUDの2P側名前表示は「CPU」の代わりに「2P」になる（`js/ui.js`の`drawHud()`に`player2Name`引数を追加し、`Game.render()`から`this.mode === "2p" ? "2P" : "CPU"`を渡している）
- 決着画面の勝者表示も「YOU WIN / CPU WINS」ではなく「1P WINS / 2P WINS」になる（`Game.getWinnerLabel()`が`this.mode`を見て文言を切り替える）
- ラウンド制・タイマー・SUPERゲージ・ヒットストップ・ノックバック・ダウン・K.O.演出など、他のシステムはすべて`"vs"`モードと共通（CPUが人間に置き換わるだけで、ゲームルール自体は変わらない）
- 練習モードと異なり、タイトルへ戻るBackspaceショートカットは無い（決着後は`R`キーでのリマッチのみ）

### 入力ソースの抽象化（`js/inputSource.js`）
- VS 2Pモードまでは、`handleInput()`等の入力処理メソッド群は「キー割り当て表（`P1_KEYS`/`P2_KEYS`）とブラウザのキー状態（`isKeyDown()`/`isKeyJustPressed()`）」を直接受け取っていたが、後述のオンライン対戦ではキーボードの代わりにネットワーク越しに届いた操作状態を同じように扱う必要が生じたため、`InputSource`という共通インターフェース（`{ isDown(action), isJustPressed(action) }`。action名は`left`/`right`/`up`/`down`/`punchLight`など）を導入し、`handleInput()`等はすべてこちらを受け取る形に統一されている
- `createLocalInputSource(keyMap)`: ローカルのキーボード状態を`isKeyDown()`/`isKeyJustPressed()`経由でそのまま返すラッパー。`Game`は`this.player1Input`（常に`P1_KEYS`）と`this.player2LocalInput`（`P2_KEYS`、VS 2P専用）の2つを持つ
- `createRemoteInputSource()`: `applyState(state)`で渡された`{left: true, ...}`のようなプレーンオブジェクトを保持し、1つ前の状態との差分で`isJustPressed()`相当を判定する。`Game.remoteInput`として、オンライン対戦のホスト側が参加側の操作を反映するのに使う
- `captureLocalActionState(keyMap)`: ローカルの現在のキー状態を、送信用の`{action: boolean}`のプレーンオブジェクトへスナップショットする。オンライン対戦の参加側が自分の入力をホストへ送るのに使う

### オンライン対戦（簡易版）モード（`Game.mode === "online"`, `js/network.js`, `js/inputSource.js`）
- タイトル画面のONLINEボタンから開始する、別々のPC・ブラウザ間でネットワーク越しに対戦するモード。[PeerJS](https://peerjs.com/)が提供する無料の公開シグナリングサーバーを使ってWebRTCのP2P接続（DataChannel）を確立する。このプロジェクトはVercelでの静的配信のみで自前のサーバーを持たないため、シグナリングは全面的にPeerJSの公開サーバーに依存している（**PeerJSの無料サーバーが利用できない場合、この機能は動作しない**）
- **接続方式（合言葉/ルームコード）**: ホスト役は`js/network.js`の`NetworkSession.host()`で`"kogafighter-" + ランダムな4文字コード`（紛らわしい文字を除いた`ROOM_CODE_CHARS`から生成）をPeer IDとして登録し、そのコードを口頭やチャットで相手に伝える。参加役は`NetworkSession.join(code)`でそのPeer IDへ直接`connect`する（`{ reliable: false }`。低遅延優先で、古いデータの再送によるラグを避けるため）
- **接続UI（`index.html`の`#onlinePanel`、`js/main.js`）**: canvas（Canvas2D）は文字入力欄を持てないため、合言葉の表示・入力は通常のHTML要素（ボタン・input）をcanvasの上に重ねて表示する形で実装している。`Game.introPhase === "onlineLobby"`の間だけ`#onlinePanel`を表示し（`main.js`のループ内で毎フレームチェック）、「部屋を作る」「参加する」「戻る」の3ボタンと、ホスト側のコード表示・参加側のコード入力欄を切り替え表示する
- **ホスト権威方式（状態ストリーミング）**: 真のロックステップ方式（双方が同じ入力を同じフレームで処理し、シミュレーション結果が両者で完全一致することに依存する）は、わずかな非決定性（浮動小数点差異・タイミングのズレなど）でも両者の状態がズレる（desync）リスクがあり、実装・デバッグが難しい。そのため「簡易版」としてより単純で頑健な方式を採用した：
  - **ホスト**が実際のゲームシミュレーション（`Game.updateLocalSimulation()`）を担当する。1P（`player1`）はホスト自身のキーボード入力（`this.player1Input`）、2P（`player2`）は参加側から受信した入力（`this.remoteInput`、`handleNetworkMessage()`で`{type: "input"}`メッセージを受けるたびに`applyState()`で更新）で動かす
  - ホストは毎フレーム、シミュレーション後の全状態を`buildStateSnapshot()`でプレーンオブジェクト化し、DataChannel経由で参加側へ送信する（`Game.update()`内、`onlineRole === "host"`のときのみ）。送信内容は両プレイヤーの位置・向き・HP・各種タイマー・攻撃状態（`serializePlayer()`/`serializeAttack()`）、弾（`serializeProjectile()`）、ラウンド進行状況など、描画に必要な情報一式
  - **参加側はシミュレーションを一切行わない**。`updateJoinerNetworking()`では自分のローカル操作（常に`P1_KEYS`基準。物理的なキーボードは1人1台なので、ホスト側か参加側かに関わらず各自のマシンでは常に`P1_KEYS`を使う）を`captureLocalActionState()`でスナップショットし、`{type: "input"}`としてホストへ送るだけ。ホストから届いた`{type: "state"}`スナップショットは`applyStateSnapshot()`で自分の`player1`/`player2`/`projectiles`等へそのまま反映し（`Object.assign`）、それを`render()`でただ描画する「受動的なビューア」として振る舞う
  - この方式はdesyncが原理的に起こらない（参加側は常にホストの状態をそのまま表示するだけ）反面、**参加側自身の操作にも往復の通信遅延（RTT）分の見た目上のラグが乗る**（自分の入力→ホストに届く→ホストが処理→結果が返ってくる、という一往復が必要なため）。これは「簡易版」として許容したトレードオフであり、対戦相手同士の物理的な距離やネットワーク状況によって体感が大きく変わる
- **役割の決定**: `Game.beginOnlineMatch(role, network)`（`role`は`"host"`または`"joiner"`）が接続成立後に呼ばれ、`Game.onlineRole`・`Game.network`をセットし`Game.mode = "online"`にした上で`introPhase`を`"ready"`に進める。以降`Game.update(dt)`は`onlineRole`に応じて`updateLocalSimulation()`（ホスト）または`updateJoinerNetworking()`（参加側）のどちらを毎フレーム呼ぶかを切り替える
- HUDの2P側名前表示・決着画面の勝者表示（「1P WINS / 2P WINS」）はVS 2Pモードと同様の扱いになる（`this.mode === "2p" || this.mode === "online"`の判定を共通で使っている）
- **切断時の挙動（簡易版の制約）**: 接続が切れると`handleNetworkDisconnected()`が呼ばれ、再接続やロールバックといった復帰処理は一切行わない。`Game.onlineContext`が`"standalone"`（このモード単体で開始した場合）ならタイトル画面へ、`"room"`（後述のルーム機能内の対戦・観戦だった場合）なら`Game.onReturnToRoom()`経由でルームのロビー画面へ戻る（`Game`自身は`js/room.js`の存在を知らず、ルーム機能側が`beginOnlineMatch()`の前後で`onlineContext`/`onReturnToRoom`をセットすることで実現している）
- **既知の制約（今後の課題）**:
  - 参加側の操作にRTT分のラグが乗る（上述）。ロールバック等の遅延隠蔽は未実装
  - 切断時の再接続機能が無い（対戦を中断すると最初からやり直しになる）
  - PeerJSの無料公開シグナリングサーバーに依存しており、可用性・安定性の保証は無い
  - NAT越えに失敗するネットワーク環境（一部の厳格なファイアウォール等）では接続できない場合がある（TURNサーバーは未設定）

### ルーム機能（`js/room.js`の`RoomHost`/`RoomMember`、最大8人・対戦枠2つ・観戦・チャット）
- タイトル画面のONLINEボタン→オンライン接続画面の「ルームを作る」「ルームに参加する」から開始する。上記の1対1オンライン対戦（合言葉のみ）とは別の入口・別のPeer ID名前空間（`"kogafighter-room-" + 4文字コード`。1対1の`"kogafighter-" + 4文字コード`とは衝突しない）を持つ、最大8人が同じ部屋に入れる拡張機能
- **バックエンドサーバーを持たない設計方針を維持**するため、部屋を作った人のブラウザ（Peer）が幹事役として全員のメッセージ（メンバー一覧・対戦枠の状態・チャット）を中継する。**部屋作成者がタブを閉じる／回線が切れると部屋全体が終了する**（1対1オンライン対戦のホスト権威方式と同じ考え方の延長。受け入れ済みのトレードオフ）
- **トポロジ**: 参加者ごとに1つの`Peer`オブジェクトを持つ。部屋作成者のPeerが最大7人分の「部屋コネクション」（`{purpose:"room", name}`というmetadata付きで接続）を受け付け、メンバー一覧・対戦枠の状態・チャットを配信する。2人が同じ対戦枠に入ると、先に入った方が「対戦ホスト」になり、後から入った方が**自分の既存のPeerオブジェクト上に**対戦ホストへの2本目のDataConnection（`{purpose:"match"}`）を追加で張る（部屋コネクションとは別本数）。観戦者も同様に対戦ホストへ3本目の種類のコネクション（`{purpose:"spectate"}`）を直接張る
- **接続の用途の見分け方**: 着信コネクションの`conn.metadata.purpose`（`"room"`/`"match"`/`"spectate"`）で即座に振り分ける（`peer.connect(id, {metadata})`のmetadataはシグナリング経由で`conn.open`前に同期的に読めるため、低遅延（`reliable:false`）チャンネルでの初回メッセージ到達順に依存する必要がない）
- **対戦本体の通信は`js/network.js`の`NetworkSession`をそのまま流用**し、対戦・観戦のコード自体は一切変更していない。そのための拡張が`NetworkSession.attachToConnection(peer, conn, role, {ownsPeer})`（新規Peer作成をスキップして既存のPeer/コネクションへ後付けでアタッチする。`ownsPeer:false`のとき`close()`は`peer.destroy()`しない＝部屋コネクションを道連れにしない）と、`spectatorConnections`配列＋`addSpectator()`/`removeSpectator()`（`send()`が対戦相手に加えて観戦者全員にもファンアウトする。`applyStateSnapshot()`が元々フル状態コピーだったため、観戦を試合途中から開始しても特別なブートストラップ処理は不要）
- **Gameとの連携**: `Game.onlineRole`に`"spectator"`を追加し、`updateSpectatorNetworking()`（`{type:"input"}`を送らない以外は`updateJoinerNetworking()`と同じ）で動く。ルーム機能側は`beginOnlineMatch()`を呼ぶ直前に`Game.onlineContext = "room"`と`Game.onReturnToRoom`（部屋のロビーへ戻る処理）をセットする。`Game`自体は`js/room.js`を一切importしない疎結合を保っている
- **決着画面のボタン出し分け（`Game.getMatchResultButtons()`）**: 通常対戦は「もう一度対戦／練習する／タイトルへ／CPUと戦う」の4択、ルーム対戦のプレイヤーは「もう一度対戦／ルームに戻る」の2択、ルーム対戦の観戦者は「観戦をやめる」の1択。`js/ui.js`の`getResultButtonRect(index, count, ...)`がボタン個数に応じて横並び位置を計算する汎用形になっている
- **接続UI（`index.html`の`#roomPanel`、`js/main.js`）**: `Game.introPhase === "room"`の間だけ表示するHTMLオーバーレイ（`#onlinePanel`と同じ前例に倣う）。合言葉・人数表示、対戦枠2つ（それぞれ空き/募集中/対戦中の状態に応じて「参加する」「観戦する」「抜ける」ボタンを出し分け）、メンバー一覧、チャットログ＋入力欄で構成する。動的に増減するリスト系UIのためCanvas手描きではなくDOMで実装している
- **メッセージプロトコル**: 部屋コネクションでは`{type:"joinSlot"|"leaveSlot"|"spectateRequest"|"spectateStop"|"chatMessage"}`（メンバー→部屋ホスト）、`{type:"roomState"|"chatMessage"|"joinRejected"|"matchAssignment"|"spectateAssignment"}`（部屋ホスト→メンバー、チャット以外は`roomState`に含めず都度配信）。対戦/観戦コネクションは1対1オンライン対戦と同じ`{type:"input"|"state"|"rematchRequest"}`をそのまま使う
- **既知の制約（今後の課題）**:
  - 部屋作成者が退室すると部屋全体が終了する（上述）。ただし進行中の対戦・観戦は部屋作成者を経由しないため、既に成立している対戦自体はそのまま続行できる
  - 対戦ホスト（または観戦対象）が不意にタブを閉じた場合、相手・観戦者はWebRTCのICE切断タイムアウト（数秒〜）まで気づけない。意図的な退室（「ルームに戻る」等のボタン操作）の場合は`closeNetworkSilently()`が即座に接続を閉じるため、正常系はほぼ即時に伝わる
  - 1人が「対戦枠のプレイヤー」と「別枠の観戦者」を同時に兼ねることはできない（`RoomHost`側で`spectateRequest`/`joinSlot`を相互に拒否する）
  - チャットログは追記型で配信するのみで、部屋ホストは履歴を保持しない。途中から入室したメンバーは過去のチャットを見られない。表示側（`js/main.js`）も直近50件を超えると古い表示から間引く
  - 表示名は自由入力（省略可、既定"Player"）で他メンバーとの重複チェックは無い

### 練習モード（`Game.mode === "practice"`）
- タイトル画面のPRACTICEボタンから開始する、CPU（P2）が一切行動しないモード。用途は自由なコマンド練習・コンボ確認・ダメージ確認など
- CPUの思考（`CpuController.update()`）自体を呼ばない（`update()`内で`this.mode !== "practice"`のときのみ`this.cpu.update(dt)`を実行）。P2は自ら動かず攻撃もガードもしないが、`Player.update()`自体は通常通り呼ばれるため、ヒット硬直・ガード硬直・重力といった「殴られたときの反応」は通常戦と同様に機能する（＝殴りごたえのある的として機能する）。また`updateFacing()`は無条件で毎フレーム呼ばれるため、P2はCPU思考が無くても自動的にP1の方を向く
- ラウンドタイマーが減らない（`update()`内で`this.mode !== "practice"`のときのみ`timeRemaining`を減算）。HUDのタイマー表示は`timeRemaining`に`null`を渡すことで「∞」が表示される（`drawTimer()`）
- HPが0になっても決着せず、その場で全回復する（`checkRoundEnd()`の代わりに`updatePracticeHealing()`を呼び、`hp <= 0`のプレイヤーを即座に`maxHp`へ戻す）。ラウンド制・SUPERゲージ等の他のシステムには影響しない
- ラウンド概念が無いため、HUDの獲得ラウンド数の丸印（`roundWins`/`roundsToWin`）は非表示、READY演出も「ROUND N」ではなく通常の「READY」表示になる（どちらも`practice`時は`null`を渡すことで、既存のフォールバック表示に任せている）
- 画面下部に「練習モード（Backspaceキーでタイトルへ）」という案内文を常時表示する
- **Backspaceキー**を押すといつでも`Game.returnToTitle()`が呼ばれ、`introPhase`が`"title"`に戻り`mode`も`"vs"`にリセットされる（この戻る操作は練習モード中のみ有効。通常対戦中には効かない）

## 7.5. 試合開始演出（`Game.introPhase`, `js/ui.js`のdrawIntroOverlay()）
- タイトル画面でSTARTが押される、または各ラウンド決着後に次のラウンドが始まると`introPhase = "ready"`になり、以降は`update()`内、`introPhase !== "playing"`の間は`updateIntro(dt)`だけを呼んで即returnする（移動・攻撃・CPU思考・ラウンドタイマーの減少などは一切行われない）
- `introPhase`は「`ready`（`READY_DURATION`=1.2秒、仮値）→ `fight`（`FIGHT_DURATION`=0.8秒、仮値）→ `playing`」の順に自動で進む。`playing`になって初めて通常の対戦更新が始まる
- 画面は暗転させず、両キャラクターは初期位置に向かい合った状態で表示されたまま、中央に大きく「ROUND N」（白。`this.currentRound`の値を使う）→「FIGHT!」（赤、より大きく）の文字を重ねて表示する（`drawIntroOverlay()`）
- ESCキーでの操作説明表示（`showHelp`）はREADY/FIGHT演出中でも有効（`update()`内でintroの判定より先にチェックされる）。タイトル画面中（`introPhase === "title"`）はこのチェック自体を通らないため無効

## 8. 勝敗判定・ラウンド制（`js/game.js`, `js/ui.js`）

### ラウンドの決着（`Game.checkRoundEnd()`）
- いずれかのHPが0になったフレーム、または`timeRemaining`が0になったフレームで1ラウンドの決着とみなし、`introPhase = "roundEnd"`に遷移する（`checkRoundEnd()`。`update()`の通常更新の最後で毎フレーム呼ばれる）
- そのラウンドの勝者は `this.winner` に記録。K.O.で両者同時に0になった場合、またはタイムアップでHPが同値の場合は `null` = そのラウンドは引き分け扱い（`roundWins`はどちらも増えない）。タイムアップ時はHPが多い方がそのラウンドの勝者になる
- 決着理由は `this.endReason`（`"ko"` / `"timeup"`）に記録される
- ラウンドの勝者が決まると`this.roundWins.player1`または`.player2`が1増える

### ラウンド間の進行（`Game.advanceRound()`, `ROUND_RESULT_DURATION`）
- ラウンド決着後、`ROUND_RESULT_DURATION`（2.0秒、仮値）の間`js/ui.js`の`drawRoundResult()`でそのラウンドの「K.O./TIME UP」＋勝者名を表示する（暗転なし・リスタート案内もない軽量な表示。次のラウンドへ自動で進むため）
- 表示時間が経過すると`advanceRound()`が呼ばれる。どちらのプレイヤーも`ROUNDS_TO_WIN`（2本、仮値＝3本勝負/best of 3）に達していなければ、`startRound()`で次のラウンドを始める（`currentRound`をインクリメントし、両プレイヤー・タイマー・弾を初期状態にリセットして`introPhase = "ready"`に戻す。`roundWins`はリセットしない）
- どちらかが`ROUNDS_TO_WIN`に達していれば、そこでマッチ全体が終了する（`this.matchOver = true`、`introPhase`は"playing"に戻して以降は通常の`matchOver`処理に委ねる）

### マッチ全体の決着（`Game.matchOver`）
- `matchOver = true`になった時点の`this.winner`が、そのままマッチ全体の勝者になる（先取本数に達した決定的なラウンドの勝者と一致するため、別途「マッチ勝者」を計算し直す必要はない）
- 決着時は画面が暗転し、`js/ui.js` の `drawMatchResult()` が「K.O.」または「TIME UP」＋「YOU WIN / CPU WINS / DRAW」＋獲得ラウンド数のスコア（例: `2 - 1`）＋文脈に応じた選択肢ボタン（後述）を表示する
- `matchOver` 中に `R` キーを押すと（ローカルモード、およびオンライン対戦のホスト側でのみ有効）`restart()`が呼ばれ、`roundWins`と`currentRound`を初期値に戻した上で`startRound()`を呼び、両プレイヤー・タイマー・SUPERゲージを初期状態で再生成する（タイトル画面には戻らず、直接ラウンド1の「READY」から始まる）

#### 決着画面の選択肢ボタン（`Game.getMatchResultButtons()`/`handleMatchResultClick()`, `js/ui.js`の`getResultButtonRect()`）
- ボタンの構成は対戦の文脈で変わる（`getMatchResultButtons()`が`[{key, label}, ...]`を返す）。通常対戦（`"vs"`/`"2p"`/単体オンライン対戦）は「もう一度対戦／練習する／タイトルへ／CPUと戦う」の4択、ルーム機能の対戦（`Game.onlineContext === "room"`）は「もう一度対戦／ルームに戻る」の2択、ルーム機能の観戦者（`onlineRole === "spectator"`）は「観戦をやめる」の1択。`js/ui.js`の`getResultButtonRect(index, count, canvasWidth, canvasHeight)`はボタン個数に応じて横並び位置を計算する汎用形になっており、描画・クリック判定・ホバー判定のすべてで同じボタン構成・同じ関数を共有する
- **もう一度対戦**: 同じモードのまま`restart()`相当の処理を行う（`Game.rematchAfterMatch()`）。オンライン対戦の参加側だけは自分でシミュレーションを行わないため、直接`restart()`を呼ぶ代わりに`{type: "rematchRequest"}`メッセージをホストへ送り、ホスト側で`restart()`が呼ばれた結果が次のスナップショットで反映される
- **練習する** / **CPUと戦う**（通常対戦のみ）: `Game.leaveToMode("practice" | "vs")`が呼ばれる。オンライン対戦中であれば`closeNetworkSilently()`で接続を閉じてから、獲得ラウンド数をリセットして指定モードで`startRound()`する
- **タイトルへ**（通常対戦のみ）: `Game.goToTitleAfterMatch()`が呼ばれる。同様に接続があれば閉じてから`introPhase = "title"`・`mode = "vs"`に戻す
- **ルームに戻る** / **観戦をやめる**（ルーム機能のみ）: `Game.returnToRoomAfterMatch()`が呼ばれる。接続を閉じてから`Game.onReturnToRoom()`（ルーム機能側がセットしたコールバック）を呼び、部屋のロビー画面へ戻す
- **オンライン対戦中に接続を自分から閉じる場合の注意（`closeNetworkSilently()`）**: `NetworkSession.close()`を呼ぶ前に`network.onDisconnected`を`null`にしておく。理由は、`close()`に伴ってPeerJS側の切断イベントが（非同期に）発火し、それをそのまま`handleNetworkDisconnected()`に流すと、直後に自分でセットしたはずの遷移先の状態（`leaveToMode()`等で設定した`mode`/`introPhase`）を上書きしてしまうレースコンディションになるため
- ボタンとも`Game.matchOver === true`の間だけクリック・ホバー判定が有効（`handleClick()`は`matchOver`を`introPhase === "title"`の判定より先にチェックする）

### HUDの獲得ラウンド数表示（`js/ui.js`のdrawRoundPips()）
- 各プレイヤーのHPバー左上（P2は右上）に、`ROUNDS_TO_WIN`個の丸印を表示する。獲得済みラウンドの丸は黄色で塗りつぶし、未獲得は白の輪郭のみ（`drawHud()`に`roundWins`・`roundsToWin`を渡さなければこの表示自体が省略される設計になっている）

## 9. 未実装・今後の課題

以下は現状のコード・コメントから確認できる「まだ実装されていない」項目。

- CPUの高度な読み合い・難易度調整（詳細は7章「CPUが行わないこと」参照）
- ダウンからの起き上がりモーション（ダウン状態自体・ダウン中の無敵は実装済み。8章「ダウン（転倒）状態」参照）
- 本格的なコンボシステム（ダメージ補正・コンボカウンター表示など。ヒットストン中に追撃すれば連続ヒット自体はする）
- 波動拳・SA・昇龍拳以外の必殺技（飛び道具・対空以外のタイプなど）
- パンチ・キック等の専用アニメーション画像（P1の弱・強パンチ、波動拳、昇龍拳、ダウンには対応済み。それ以外の技・P2側は見た目だけの前後移動で代替。5章「攻撃モーション」参照）。立ち絵・しゃがみ絵・歩行コマの複数枚表示には対応済み。ポートレートは自作の簡易アイコンのまま

## 10. 未確定事項（要確認・暫定実装）

- 開発が「フェーズ」単位で進行している形跡がある（`CROUCH_PUNCH_DATA`のコメント参照）が、フェーズ全体の計画がこのリポジトリ内に見当たらない。全体のロードマップがあれば別途反映する。
- **CPUの強さ・行動パターンは仮実装**。`js/ai.js`内の各種確率・距離・時間の定数（`ACTION_WEIGHTS`の重み配分、`MODE_ATTENTION`の反応倍率、`CLOSE_RANGE` / `FAR_RANGE` / `REACTION_RANGE` / `JUMP_IN_RANGE` / モード継続時間など）はいずれも独自の仮値で、難易度段階（弱いCPU/強いCPU切り替え）も未検討
- **しゃがみ強Kは本来の「ダウンを奪う」効果に置き換え済み**（`causesKnockdown: true`）。ダウンの吹き飛び・倒れ各局面の長さ（0.35秒/1.0秒）は仮値で、後日調整の可能性がある
- **ヒットストップ（`HITSTOP_HIT_DURATION`=0.08秒/`HITSTOP_BLOCK_DURATION`=0.04秒）・ノックバック（`KNOCKBACK_INITIAL_SPEED`=500px/秒、`KNOCKBACK_FRICTION`=2500px/秒²）はいずれも技の強さによらず一律固定の仮値**。技ごとに変える場合は`combat.js`の技データに専用フィールド（例: `hitstopDuration`、`knockbackSpeed`）を追加する拡張が必要
- CPUの行動選択（`decide()`）は相手の「攻撃硬直(recovery)中」への反撃は考慮しているが、「ヒットストン中で確実に反撃を差し込める」状況を専用に優先する仕組みはまだ無い（結果的に間合い内であれば攻撃は選ばれやすいが、意図した設計ではない）
- **ガードの削りダメージ率（`GUARD_CHIP_RATIO`=10%）も仮値**。バランス調整は今後の課題
- **SUPERゲージの増加率（`SUPER_GAUGE_GAIN_RATE`=0.03）・満タン基準（100）も仮値**。1〜4ヒット程度で満タンになる想定だが、他のコマンド技の実装が揃った際にまとめて調整する方針（前述の「コマンド技の火力バランス調整は保留中」と合わせて見直す）
- **ラウンドタイマー（`ROUND_TIME`=99秒）も仮値**。実際のプレイ時間感を見て調整の余地がある
- **波動拳コマンド入力中の歩行抑制（`isInSpecialMotionGrace()` / `MOTION_GRACE`）は応急的な実装**。しゃがみ状態が実装されればSキー押下で自然に歩行が止まる設計に置き換わる見込みで、後日修正される可能性が高い
- 波動拳はパンチボタン（U/I/O）どのボタンでも同じ性能で発生する（ボタンの強弱による弾速・ダメージ差は未実装）
- **コマンド技（必殺技・SA）の火力バランス調整は保留中**。方針として、コマンド技を全て実装してからまとめて調整する。それまでは各技のダメージ値を個別に変更しない
- **しゃがみ時の高さ（`CROUCH_HEIGHT = 150`）は仮値**。参考にしたフレームデータ表に記載がないため独自に設定したもので、見た目のバランスを見て後日調整される可能性が高い
- しゃがみ導入後も「Sキー押下中は歩行禁止」ロジック（`handleInput()`内）と「しゃがみ状態そのもの」の役割が一部重複しており、整理の余地がある
- **画像アセットの既知の落とし穴（運用メモ）**: 「スクリーンショット」経由で用意されたPNGは、透過ビューアの市松模様がそのまま不透明なピクセルとして焼き込まれ、アルファチャンネルが全面255（＝透過なし）になっていることが毎回発生している（P1しゃがみ絵・P1歩行コマ6枚・P1立ち絵・P1強パンチ2枚・P1弱パンチ2枚・P1波動拳4枚・P1しゃがみ遷移コマ2枚・P1ガード絵・P1被弾絵・P1ジャンプ5枚で発生）。取り込み前に必ずアルファ値を確認し、必要なら背景を除去してから`assets/`に配置している。画像生成ツール（Gemini等）が直接生成した画像でも、市松模様の代わりに単色に近い背景（ほぼ白など）が焼き込まれているだけで同じ問題が起きることがある（P1ガード絵で確認）。画像生成ツールから直接「背景透過PNG」として書き出せる場合はこの問題は発生しない
- **背景除去の手法（運用メモ）**: 基本はPython(Pillow + scikit-image)の`flood`で画像の四辺からグレースケール値のトレランス一致でフラッドフィルする方式を使っている。ただし、キャラクターの衣装の陰影（白い道着の影など）が背景と同じグレー階調域に重なっている画像では、この方式だと陰影部分まで背景と誤認されて服に穴が開くことがある（P1ジャンプ用の空中3枚で発生・確認）。この場合は`rembg`（`pip install rembg`、U2-Netという学習済みモデルを使うAI背景除去ライブラリ）に切り替えると、色の閾値ではなく被写体のシルエットを認識して切り抜くため、同じグレー階調が重なっていても服を壊さずに背景だけを正しく除去できる。初回実行時にモデル(`u2net.onnx`、約176MB)をGitHubからダウンロードするため数十秒かかる（`~/.u2net/`にキャッシュされ2回目以降は不要）。以後、単純なflood方式で服や髪に穴が開くようであれば、最初から`rembg`を使う方が手戻りが少ない
- **`rembg`自体の落とし穴（運用メモ）**: `rembg`はシルエット認識モデルのため、逆に本体から離れて細く尾を引く装飾（例: 遠くまで流れる長い後れ毛の房）や、真っ直ぐ伸びて画面端まで届く腕・拳（例: P1弱パンチ`active1`の伸びきったジャブの前腕〜拳）を「背景」と誤判定し、半透明〜ほぼ透明にしてしまうことがある（P1ジャンプ`fall1`・P1弱パンチ`active1`で発生。`alpha_matting=True`のトリマップ精緻化オプションを試しても改善しなかった）。この種の欠けは市松模様の除去漏れと違って地味に見えにくいので、`Image.alpha_composite`で**マゼンタ等の派手な単色背景**に合成して確認すると気づきやすい（緑や白だと元の配色と被って見落としやすい）。対処法として、元画像のグレースケール値が背景基準色から一定以上（例: 45階調）離れている画素は「明らかに背景色ではない」とみなし、`rembg`がその画素を透明にしていても強制的に不透明へ復元するハイブリッド処理を行った（`js/`のコードではなく画像の前処理段階の話）
- **発光・煙などの半透明エフェクト画像の背景除去（運用メモ）**: キャラクター立ち絵と違い、波動拳の弾のような発光エフェクトは本来ふちが完全な不透明ではなく、淡く発光しながら透明にフェードアウトする見た目が意図されている。単純な二値（不透明/透明）のフラッドフィルだと発光の階調が失われるため、この場合は「無彩色（背景の市松模様、r≈g≈bのグレー）に近いほどアルファを下げ、青み・明るさが強いほどアルファを上げる」計算でグラデーションのあるアルファチャンネルを作る方式を使った（`(g+b)/2 - r` の「青み」と `(r+g+b)/3 - 背景基準値` の「明るさ超過」の大きい方を信号として使用）。市松模様の明暗2色の輝度差だけでも意外と信号にノイズが乗る（境界付近で二値の閾値を低く取りすぎると市松模様がうっすら透けて残る）ため、実際の背景サンプルと発光部分のサンプルを数値で比較しながら閾値を決めるとよい
